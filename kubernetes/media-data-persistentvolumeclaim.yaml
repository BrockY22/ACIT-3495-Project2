apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: media-data
  name: media-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
